Reproducing https://github.com/nmerovingian/ECINN.

```
pip install -r requirements.txt
mkdir images
python main.py
```

The images representing how the model fits the data will appear in the `images` folder. 